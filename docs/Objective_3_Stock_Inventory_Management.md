# Objective 3: Stock Inventory Management Module
**Owner:** Christine P. De Padua
**Project:** CakeLytics

## Objective Description
This objective covers the Stock Inventory Management Module of CakeLytics, which enables the business owner to manage all inventory-related information such as raw ingredients, celebration materials, recipes, produced products, and waste. This objective also includes setting up the Supabase authentication system for the owner login and designing the shared database structure that all other modules depend on, making it the foundation of the entire CakeLytics system.

## 5-Week Task Breakdown (Editable Development Plan)

| Week / Day | Task Description | Sub-Tasks (breakdown) | Deliverable(s) | Test Suite / PR Acceptance Criteria |
| :--- | :--- | :--- | :--- | :--- |
| **Week 1 <br> Day 1** | Backend Foundation, Frontend Foundation, Mock Schema, Skeleton Routes, Mock Authentication Service, and Admin Login UI | 1. Set up backend and Frontend project folder structure and install dependencies<br>2. Design the mock database schema covering all inventory tables and attributes<br>3. Build skeleton routes file with stub controllers<br>4. Build the mock authentication service with login and logout functions<br>5. Build the auth middleware that verifies a token before allowing access to protected routes<br>6. Wrap the skeleton routes with the auth middleware<br>7. Build the Admin Login form UI and connect it to the mock authentication service | 1. Backend and Frontend project workspace with folder structure and dependencies installed<br>2. Mock database schema file defining all table structures and attributes<br>3. Skeleton routes with stub controllers<br>4. Mock Authentication service (login, logout)<br>5. Auth middleware function<br>6. Protected skeleton routes<br>7. Admin Login Form component | 1. It should return a valid session token when login is called with correct admin credentials.<br>2. It should reject the login attempt when credentials are incorrect.<br>3. It should reject the login attempt when email or password is missing.<br>4. It should clear the active session when logout is called.<br>5. It should return false from logout when the token does not exist.<br>6. It should return null from verifyToken for an invalid or unknown token.<br>7. It should update the input state when the admin types into the email or password field.<br>8. It should call the mock login function with the entered credentials when the form is submitted.<br>9. It should store the token and call onLogin with the admin info on success.<br>10. It should show an error and not call onLogin when login fails.<br>11. It should show validation errors and not call the login service when fields are empty.<br>12. It should render and run the main application frontend without crashing. <br>13. It should resolve with mock admin data and save the token when credentials match the seed definitions.<br>14. It should reject the login attempt with a validation error when incorrect credentials are provided.<br>15. It should clear the local session token and resolve successfully during a simulated logout request.<br>16. It should call the live fetch endpoint, update local storage tokens, and return data when a network login is successful.<br>17. It should intercept failure states and throw the exact error message returned by the backend API when the server response is invalid.<br>18. It should fire a secure live network fetch to the logout endpoint and completely wipe the local token upon logging out. |
| **Week 2 <br> Day 1** | Creating All backend services of Inventory | 1. Create celebration materials services. <br>2. Create production log services.<br>3. Create recipe log services.<br>4. Create waste log services.<br>5. Create raw ingredients services. | 1. created all inventory services |<br>1. It should return all ingredients successfully.<br>2. It should return an empty array when there are no ingredients.<br>3. It should return an error when findAll fails.<br>4. It should return the created ingredient on success.<br>5. It should return an error when the model fails to create.<br>6. It should return an error when the body is empty or null.<br>7. It should return the updated ingredient on success.<br>8. It should return an error when the ingredient cannot be found during update.<br>9. It should return an error when findById or setStock fails during restock.<br>10. It should handle zero restock quantity without returning an error.<br>11. It should successfully delete the ingredient.<br><br>12. It should return all materials successfully.<br>13. It should return an empty array when there are no materials.<br>14. It should return an error when findAll fails.<br>15. It should return the created material on success.<br>16. It should return an error when the model fails to create.<br>17. It should return an error when the body is empty or null.<br>18. It should return the updated material on success.<br>19. It should return an error when findById or setStock fails during restock.<br>20. It should handle zero restock quantity without returning an error.<br>21. It should successfully delete the material.<br><br>22. It should return all recipes successfully.<br>23. It should return an empty array when there are no recipes.<br>24. It should return an error when findAll fails.<br>25. It should return the recipe when found by id.<br>26. It should return an error when getById cannot find the recipe or if findById fails.<br>27. It should return the created recipe with its ingredients on success.<br>28. It should handle an empty ingredients array without returning an error.<br>29. It should return an error when the recipe row cannot be created or when insertIngredients fails.<br>30. It should clear all ingredients when an empty array is passed on update.<br>31. It should return an error when insertIngredients fails during update.<br>32. It should successfully delete the recipe and return an error when delete fails.<br><br>33. It should return all production logs successfully.<br>34. It should return an empty array when there are no production logs.<br>35. It should return an error when findAll fails.<br>36. It should correctly deduct quantities from both inventory tables based on batch count.<br>37. It should correctly compute the deduction quantity per batch.<br>38. It should only deduct from ingredients when all recipe items are raw.<br>39. It should only deduct from materials when all recipe items are materials.<br>40. It should return an error when insertDeductions fails after log creation.<br>41. It should return the production log on success.<br><br>42. It should return all waste logs successfully.<br>43. It should return an empty array when there are no waste logs.<br>44. It should return an error when findAll fails.<br>45. It should deduct from ingredients and not materials when waste type is ingredient (passing correct name and quantity).<br>46. It should deduct from materials and not ingredients when waste type is material (passing correct name and quantity).<br>47. It should return an error when create fails after ingredient or material deduction.<br>48. It should return the created waste log on success. |
| **Week 2 <br> Day 2** | All Inventory UI Components (Raw Ingredients, Celebration Materials, Recipe Log, Production Log, Waste Log) | 1. Build the Raw Ingredients page with functions for viewing, adding, editing, deleting, and restocking ingredients<br>2. Build the Celebration Materials page with functions for viewing, adding, editing, deleting, and restocking materials<br>3. Build the Recipe Log page with functions for viewing, adding, and deleting recipes, including a shopping list for insufficient stock<br>4. Build the Production Log page with functions for viewing production entries<br>5. Build the Waste Log page with functions for logging ingredient, product, and material waste | 1. Raw Ingredients list page with add/edit/delete/restock forms<br>2. Celebration Materials list page with add/edit/delete/restock forms<br>3. Recipe Log page with add/delete recipe forms and shopping list<br>4. Production Log page with list view<br>5. Waste Log page with logging forms for ingredient, product, and material waste | **RawTab**<br>1. It should render all ingredients from context, including the correct name and stock quantity<br>2. It should fall back to mock ingredient data when context returns an empty array<br>3. It should open the restock modal with the edit-mode label when Restock is clicked<br>4. should call restockIngredient with the added quantity when restocking<br>5. It should call addIngredient with the correct initial data when a new ingredient form is submitted<br>6. It should call deleteIngredient with the correct ingredient id when deletion is confirmed<br>7. It should display the empty-state message when a search query matches zero ingredients<br><br>**CelebrationTab**<br>8. It should render all celebration materials from context with the correct names<br>9. It should fall back to mock data and still display items when context returns an empty array<br>10. It should filter the list to only matching items when a search query is entered<br>11. It should display the empty-state message when a search query matches zero materials<br>12. It should call addMaterial with the correct shape when a new material form is submitted<br>13. should call restockMaterial with the added quantity when add stock is submitted<br>14. It should call deleteMaterial with the correct material id when deletion is confirmed<br><br>**RecipeTab**<br>15. It should show the shopping list banner when a recipe has insufficient stock<br>16. It should compute stock capacity as floor(stock / ingredientQty) * yield and handle zero capacity<br>17. should call addRecipe with the correct product_id and yield_quantity when a new recipe is saved<br>18. It should call deleteRecipe with the correct recipe id when deletion is confirmed<br><br>**ProductLogTab**<br>19. It should render all production log entries from context when data is available<br>20. It should render the produced quantity with the correct unit badge format (+N unit)<br>21. should display the default empty-state message when context returns an empty array<br>22. It should display the empty-state message when a search query matches zero production records<br>23. It should display the no-records empty-state message when logs are present but the search matches nothing<br>24. It should show pagination and still filter correctly when more than 10 logs are loaded<br><br>**WasteTab**<br>25. should call logWaste with the correct backend payload when logging ingredient waste<br>26. It should call logWaste with the estimated cost computed correctly when logging product waste<br>27. It should call logWaste with the estimated cost computed as costPerUnit * qty when logging material waste<br>28. It should display the no-records empty state when the logs array is empty and the search returns no results |
| **Week 3 <br> Day 1** | Connecting the Database to the Backend and Authentication | 1. Set up the Supabase client configuration to connect the backend to the real database<br>2. Rebuild the Auth model to query the real database instead of mock data<br>3. Rebuild the Auth service to sign in through the database and issue a JWT-based session<br>4. Rebuild the Auth controller to validate input and handle real database responses<br>5. Rebuild the Auth route and connect it with the JWT-based authentication middleware | 1. Supabase config connected to the backend<br>2. Auth model with real database queries<br>3. Auth service with database-based login and JWT session<br>4. Auth controller with validation and error handling<br>5. Auth route protected by JWT middleware | **Database Connection**<br>1. It should successfully connect to the database<br><br>**AuthModel**<br>2. It should return an error when signing in with an invalid email format<br>3. It should return an error when signing in with a non-existent email<br>4. It should return an error when signing in with a wrong password<br>5. It should return an error when fetching an admin with a non-existent id<br>6. It should return an error when fetching an admin with an invalid uuid format<br><br>**AuthService**<br>7. It should return a token and admin data when login is successful<br>8. It should throw when signIn fails<br>9. It should throw when signIn succeeds but no matching admin record exists<br>10. It should return admin data when getProfile succeeds<br>11. It should throw when getProfile cannot find the admin<br><br>**AuthController**<br>12. It should call ok() when login is successful<br>13. It should return 401 when a credential error is thrown<br>14. It should call next() for other unknown errors<br>15. It should call ok() to clear the session<br>16. It should call ok() with admin profile data<br>17. It should call next() when getProfile throws an error<br><br>**Auth Routes**<br>18. POST /auth/login should return 401 when logging in with invalid credentials<br>19. POST /auth/login should return 200 and a token when logging in with valid credentials<br>20. GET /auth/me should return 401 when accessing without a token<br>21. GET /auth/me should return 200 when accessing with a valid token<br>22. POST /auth/logout should return 200 when logging out with a valid token<br>23. POST /auth/logout should return 401 when logging out without a token |
| **Week 3 <br> Day 2** | Integration of Inventory Files to the Backend | 1. Integrate the Ingredient model and routes to the backend, connecting Supabase queries for the raw_ingredients table and validating all CRUD and restock endpoints<br>2. Integrate the Material model and routes to the backend, connecting Supabase queries for the celebration_materials table and validating all CRUD and restock endpoints<br>3. Integrate the Production model and routes to the backend, connecting Supabase queries for the production_logs table and validating batch logging with stock deductions<br>4. Integrate the Recipe model and routes to the backend, connecting Supabase queries for the recipes table (with joined products and ingredients) and validating recipe CRUD with ingredient handling<br>5. Integrate the Waste model and routes to the backend, connecting Supabase queries for the waste_logs table and validating waste logging endpoints | 1. Ingredient model and routes integrated to the backend<br>2. Material model and routes integrated to the backend<br>3. Production model and routes integrated to the backend<br>4. Recipe model and routes integrated to the backend<br>5. Waste model and routes integrated to the backend | **IngredientModel**<br>1. It should query the raw_ingredients table and return all ingredients on success<br>2. It should return an error when the ingredient is not found by id<br>3. It should return an error when the id format is invalid<br>4. It should call create with the exact payload and return the created record<br>5. It should call update with the correct id and payload<br>6. It should call setStock with the correct stock_quantity value<br>7. It should call delete with the correct id<br><br>**Ingredient Routes Integration**<br>8. GET /ingredients should return 401 without a valid token<br>9. GET /ingredients should return 200 and fetch all ingredients with a valid token<br>10. POST /ingredients should return 401 without a valid token<br>11. POST /ingredients should return 422 when validation fails (missing required fields)<br>12. POST /ingredients should return 201 when ingredient is successfully created<br>13. PUT /ingredients/:id should return 401 without a valid token<br>14. PUT /ingredients/:id should return 200 when ingredient is successfully updated<br>15. PATCH /ingredients/:id/restock should return 401 without a valid token<br>16. PATCH /ingredients/:id/restock should return 422 when qty is missing in payload<br>17. PATCH /ingredients/:id/restock should return 200 when ingredient is successfully restocked<br>18. DELETE /ingredients/:id should return 401 without a valid token<br>19. DELETE /ingredients/:id should return 200 when ingredient is successfully deleted<br><br>**MaterialModel**<br>20. It should query the celebration_materials table and return all materials on success<br>21. It should return an error when the material is not found by id<br>22. It should return an error when the id format is invalid<br>23. It should call create with the exact payload and return the created record<br>24. It should call delete with the correct id<br><br>**Material Routes Integration**<br>25. GET /materials should return 401 without a valid token<br>26. GET /materials should return 200 and fetch all materials with a valid token<br>27. POST /materials should return 401 without a valid token<br>28. POST /materials should return 422 when validation fails (missing required fields)<br>29. POST /materials should return 201 when material is successfully created<br>30. PUT /materials/:id should return 401 without a valid token<br>31. PUT /materials/:id should return 200 when material is successfully updated<br>32. PATCH /materials/:id/restock should return 401 without a valid token<br>33. PATCH /materials/:id/restock should return 422 when qty is missing in payload<br>34. PATCH /materials/:id/restock should return 200 when material is successfully restocked<br>35. DELETE /materials/:id should return 401 without a valid token<br>36. DELETE /materials/:id should return 200 when material is successfully deleted<br><br>**ProductionModel**<br>37. It should query the production_logs table with the default limit<br>38. It should respect a custom limit parameter<br>39. It should call create with the exact payload<br>40. It should call insertDeductions with each deduction mapped to the production_log_id<br><br>**Production Routes Integration**<br>41. GET /production should return 401 without a valid token<br>42. GET /production should return 200 and fetch all production logs with a valid token<br>43. POST /production should return 401 without a valid token<br>44. POST /production should return 422 when validation fails (missing required fields)<br>45. POST /production should return 201 when production batch is successfully logged<br><br>**RecipeModel**<br>46. It should query the recipes table with joined products and ingredients<br>47. It should return an error when the recipe is not found by id<br>48. It should return an error when the id format is invalid<br>49. It should only select recipe_ingredients when using findWithIngredients<br>50. It should call insertIngredients with each ingredient mapped to the recipe id<br>51. It should call deleteIngredients with the correct recipe_id filter<br><br>**Recipe Routes Integration**<br>52. GET /recipes should return 401 without a valid token<br>53. GET /recipes should return 200 and fetch all recipes with a valid token<br>54. GET /recipes/:id should return 401 without a valid token<br>55. GET /recipes/:id should return 200 and fetch specific recipe with a valid token<br>56. POST /recipes should return 401 without a valid token<br>57. POST /recipes should return 422 when validation fails (missing required fields)<br>58. POST /recipes should return 201 when recipe is successfully created<br>59. PUT /recipes/:id should return 401 without a valid token<br>60. PUT /recipes/:id should return 200 when recipe is successfully updated<br>61. DELETE /recipes/:id should return 401 without a valid token<br>62. DELETE /recipes/:id should return 200 when recipe is successfully deleted<br><br>**WasteModel**<br>63. It should query the waste_logs table with the default limit<br>64. It should respect a custom limit parameter<br>65. It should call create with the exact payload and return the created record<br><br>**Waste Routes Integration**<br>66. GET /waste should return 401 without a valid token<br>67. GET /waste should return 200 and fetch all waste logs with a valid token<br>68. POST /waste should return 401 without a valid token<br>69. POST /waste should return 422 when validation fails due to missing fields<br>70. POST /waste should return 201 when waste is successfully logged |
| **Week 4 <br> Day 1** | Login API Integration Testing (Frontend ↔ Mock API via MSW) | 1. Set up the MSW mock server (`sample-backend/server.js`, `sample-backend/index.js`)<br>2. Build the auth request handler (`sample-backend/handlers/auth.handlers.js`) simulating the real login API endpoint<br>3. Write API integration tests for the Login Page verifying the successful login flow (correct credentials, token stored in localStorage, onLogin callback fired with admin data)<br>4. Write API integration tests for the Login Page verifying the failed login flow (incorrect credentials, error message displayed, no token stored, onLogin not called) | 1. MSW mock server setup (`server.js`, `index.js`)<br>2. Auth mock handler (`auth.handlers.js`)<br>3. Login API integration test suite (`Auth.test.jsx`) | **Login API Integration**<br>1. Should login successfully if the credentials are correct.<br>2. Should display an error message if the credentials are incorrect. <br>3. Should display a generic error message if the server crashes or fails unexpectedly. |
| **Week 4 <br> Day 2** | Inventory API Integration Testing (Frontend ↔ Mock API via MSW) | 1. Build the inventory request handlers (`sample-backend/handlers/inventory.handlers.js`) simulating the real ingredients, materials, recipes, production, and waste log endpoints<br>2. Write API integration tests verifying the full CRUD flow on Raw Ingredients (create, restock, delete) against the mock handlers, including success toasts, client-side validation errors, and server-error handling<br>3. Write API integration tests verifying the CRUD flow on Celebration Materials (create, add stock, delete) against the mock handlers, including success toasts, client-side validation errors, and server-error handling<br>4. Write API integration tests verifying Recipe Log management (create, field validation, delete) against the mock handlers, including success toasts and server-error handling<br>5. Write API integration tests verifying Product Log entries are correctly read and filtered from the mock handlers<br>6. Write API integration tests verifying Waste Log entries can be created, validated, and read against the mock handlers, including insufficient-stock and server-error handling | 1. Inventory mock handlers (`inventory.handlers.js`)<br>2. Inventory API integration test suite (`Inventory.test.jsx`) | **Raw Ingredients**<br>1. Should create a new ingredient successfully when all fields are valid<br>2. Should show an error toast when the ingredient name is missing upon saving<br>3. Should show an error toast when the stock quantity is missing upon saving<br>4. Should show an error message when the server fails to create an ingredient<br>5. Should display existing ingredients on page load<br>6. Should show the no-results message when a search matches no ingredient<br>7. Should restock an existing ingredient successfully<br>8. Should show a validation error when the added quantity is empty upon saving<br>9. Should show an error message when the server fails to restock an ingredient<br>10. Should delete an ingredient successfully after confirming<br>11. Should show an error message when the server fails to delete an ingredient<br><br>**Celebration Materials**<br>12. Should create a new material successfully when all fields are valid<br>13. Should show a validation error when the item name is missing<br>14. Should show a validation error when the initial stock quantity is missing<br>15. Should show an error message when the server fails to create a material<br>16. Should display existing materials on page load<br>17. Should show the no-results message when a search matches no material<br>18. Should add stock to an existing material successfully<br>19. Should show a validation error when the added quantity is empty upon saving<br>20. Should show an error message when the server fails to restock a material<br>21. Should delete a material successfully after confirming<br>22. Should show an error message when the server fails to delete a material<br><br>**Recipe Log**<br>23. Should open and cancel the Add Recipe modal<br>24. Should create a new recipe successfully when a product, yield, and ingredient are provided<br>25. Should show a validation error and not submit when no product is selected<br>26. Should show a validation error when the yield is left empty or invalid<br>27. Should show a validation error when no ingredient row is filled in<br>28. Should show an error message when the server fails to save the recipe<br>29. Should display existing recipes on page load<br>30. Should show the empty message when there are no recipes<br>31. Should delete a recipe successfully after confirming<br>32. Should show an error message when the server fails to delete the recipe<br><br>**Product Log**<br>33. Should display existing production log entries on page load<br>34. Should show the default empty-state message when there are no production logs<br>35. Should show the no-results message when a search matches no production record<br><br>**Waste Log**<br>36. Should log ingredient waste successfully when all fields are valid<br>37. Should show a validation error when no ingredient is selected<br>38. Should show an error when the requested waste quantity exceeds available stock<br>39. Should show an error message when the server fails to log waste<br>40. Should display existing waste log entries on page load<br>41. Should show the empty-state message when there are no waste log entries
| **Week 5 <br> Day 1** | Building and Assembling the Inventory Page (Inter-Component Integration) | 1. Assemble the Inventory Page by integrating all inventory tab components (RawTab, CelebrationTab, RecipeTab, ProductLogTab, WasteTab) with shared AppContext state<br>2. Implement sub-tab navigation within the Stocks section (Raw Ingredients, Celebration Materials, Recipe Log, Product Log) and a separate top-level Waste Log tab<br>3. Verify inter-component behavior across the assembled Inventory Page (search, add, restock, delete, tab switching) using mocked context and the real child tab components<br>4. Verify validation and error-guard behavior across all sub-tabs (invalid inputs, insufficient stock, blank fields)<br>5. Verify Dynamic KPI cards and cross-tab state persistence | 1. Assembled InventoryPage component wired to AppContext<br>2. Sub-tab navigation (Stocks sub-tabs) and top-level Waste Log tab<br>3. InventoryPage inter-component integration test suite (real child tabs) | **Raw Ingredients sub-tab**<br>1. It should render the real RawTab with ingredient data and filter via search.<br>2. It should call addIngredient with the correct payload when a new ingredient is added.<br>3. It should call restockIngredient with the added quantity when restocking an existing ingredient.<br>4. It should call deleteIngredient with the correct id after confirming deletion.<br>5. It should trigger an error and not call restockIngredient when the added quantity is zero or negative.<br>6. It should not call addIngredient and should trigger an error when inputs are invalid (blank name or negative stock).<br><br>**Celebration Materials sub-tab**<br>7. It should render the real CelebrationTab with material data and filter via search.<br>8. It should call addMaterial with the correct payload when a new material is added.<br>9. It should call restockMaterial with the added quantity when adding stock.<br>10. It should call deleteMaterial with the correct id after confirming deletion.<br><br>**Recipe Log sub-tab**<br>11. It should render the real RecipeTab and populate the Product Name dropdown from context products.<br>12. It should call addRecipe with the correct product_id and yield_quantity after picking a product and ingredient.<br>13. It should call deleteRecipe with the correct id after confirming deletion.<br>14. It should open the Edit modal and correctly populate fields with existing recipe data.<br>15. It should not call addRecipe when saving with a negative yield or an empty ingredient row.<br>16. It should hide the Confirm button when the Target Goal is zero or negative.<br>17. It should call confirmBatch with the correct payload when the target goal is met and Confirm is clicked.<br>18. It should display the Shopping List with the correct shortfall when the Target Goal exceeds available stock capacity.<br>19. It should not save the recipe when a specific ingredient row has a zero or negative quantity.<br><br>**Product Log sub-tab**<br>20. It should render the real ProductLogTab and list all production entries.<br>21. It should filter to only today's entries when the "Today" date filter is selected.<br>22. It should show the empty-state message when a search matches nothing.<br><br>**Waste Log main tab**<br>23. It should display zero Total Loss and the empty state initially when there are no logs.<br>24. It should not submit the Waste Log when item selection and quantity are left blank.<br>25. It should not call logWaste when the inputted waste quantity exceeds current stock.<br>26. It should render the real WasteTab and hide the Stocks KPIs/sub-tabs.<br>27. It should populate the ingredient dropdown and call logWaste with the correct payload for ingredient waste.<br>28. It should populate the product dropdown and call logWaste with the correct payload for product waste.<br>29. It should populate the material dropdown and call logWaste with the correct payload for material waste.<br><br>**Cross-tab behavior**<br>30. It should preserve the active sub-tab state when toggling between Stocks and Waste Log.<br>31. It should display the correct Dynamic KPI numbers for Stocks based on mock data. |
| **Week 5 <br> Day 2** | Verifying Inventory Routing Integration | 1. Register the Inventory Page route in the app router<br>2. Connect the Sidebar navigation link to the Inventory route<br>3. Verify routing behavior for /inventory, including Sidebar-triggered navigation<br>4. Verify fallback behavior for unknown routes | 1. Inventory route registered in App router<br>2. Sidebar link connected to Inventory route<br>3. Inventory routing test suite | **Inventory Routing**<br>1. It should render a 404 Not Found page for unknown routes.<br>2. It should render the Login Page when navigating to /login.<br>3. It should render the Inventory Page when navigating to /inventory.<br>4. should navigate to All Orders when the Sidebar link is clicked. <br> 5.should navigate to Point Of Sale when the Sidebar link is clicked <br>6. should navigate to Product Management when the Sidebar link is clicked <br> 7. should navigate to Analytics when the Sidebar link is clicked |
| **Week 6 <br> Day 1** | Full Workflow Testing (End-to-End, Playwright, Real Backend) | 1. Set up the Playwright E2E test (`tests/inventory.spec.js`) running against the real dev server and real backend<br>2. Test the full process from logging in to managing ingredients and materials<br>3. Test the full process of creating a recipe<br>4. Test the full process of logging production and waste, and checking stock changes<br>5. Test that all the inventory pages can be navigated properly | 1. Playwright E2E test suite (`inventory.spec.js`)<br>2. CI integration running the E2E flow against the real backend | 1. It should pass the full E2E Inventory Flow |